spring:
  sql:
    init:
      mode: always
      platform: h2           # ou postgres
  jpa:
    defer-datasource-initialization: true

  data:
    web:
      pageable:
        default-page-size: 10
        max-page-size: 100
        one-indexed-parameters: false